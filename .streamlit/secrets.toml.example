# Streamlit secrets — copy to secrets.toml and set your values.
# Do not commit secrets.toml. It is in .gitignore.

# Optional: for AI Scout page (dashboard/pages/5_AI_Scout.py)
# GROQ_API_KEY = "your-groq-api-key"
