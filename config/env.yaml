# Environment and paths. Override with env vars: SOFASCORE_RAW_BASE, SOFASCORE_INDEX_PATH,
# SOFASCORE_API_BASE, SOFASCORE_LOG_DIR, SOFASCORE_DERIVED_DIR, SOFASCORE_PROCESSED_DIR, ENV.
# Paths are relative to project root unless set to an absolute path.

raw_base: "data/raw"
index_path: "data/index/matches.csv"
api_base: "https://api.sofascore.com/api/v1"
log_dir: "data/logs"
derived_dir: "data/derived"
processed_dir: "data/processed"
index_dir: "data/index"
env: "dev"
