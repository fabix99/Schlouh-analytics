# Deletion audit — what should or could be removed before GitHub

Audit of all folders and file types (including markdown). **Should** = recommended to remove. **Could** = optional; remove if you want a leaner repo.

---

## 1. Root directory

| Item | Verdict | Reason |
|------|---------|--------|
| `Microsoft Word - A Poisson Betting Model...pdf` | **Should delete** | Unrelated to project; large (~1.2 MB); already in `.gitignore` via `/*.pdf`. |
| `dq_check_stdout.txt` | **Should delete** | Generated QA artifact; in `.gitignore`. |
| `qa_extended_checks.json` | **Should delete** | Generated QA artifact; in `.gitignore`. |
| `validation_data_verbose.txt` | **Should delete** | Generated validation output; in `.gitignore`. |
| `Tactical_Preparation_Plan_Next_Opponent.md` | **Could delete** | Generic tactical prep template; not code. Keep only if you want it as reference in the repo. |
| `PREP_FOR_GITHUB.md` | **Keep** | Checklist for first push. |
| `README.md` | **Keep** | Main project readme. |

---

## 2. `archive/`

| Item | Verdict | Reason |
|------|---------|--------|
| `archive/` (folder) | **Could delete** | Empty. PUBLISH.md says internal one-off docs were removed. Deleting the empty folder keeps the tree clean. |

---

## 3. `docs/`

**Keep (core / referenced in PUBLISH.md):**  
`intention.md`, `setup.md`, `consumption.md`, `retention.md`, `backfill.md`, `ops.md`, `api_contract.md`, `api_endpoints.md`, `fixing_validation_failures.md`, `plan_production_grade.md`, `PRODUCTION_READINESS.md`, `PUBLISH.md`.

| File | Verdict | Reason |
|------|---------|--------|
| `DATA_QA_FIX_PLAN.md` | **Could delete** | One-off fix plan; superseded by work done. |
| `DATA_QA_FIX_PLAN_FOOTBALL_REFINED.md` | **Could delete** | Same. |
| `DATA_QA_REPORT.md` | **Could delete** | One-off QA report output. |
| `DATA_QA_MEGA_PROMPT.md` | **Could delete** | Long prompt for QA; only mentioned in a comment in `run_qa_extended_checks.py`. Keep if you still run that process manually. |
| `DATA_AND_PLAN.md` | **Could delete** | Internal planning. |
| `DASHBOARD_UI.md` | **Could delete** | Internal UI notes. |
| `PROMPT_UI_OVERHAUL.md` | **Could delete** | Internal prompt/UI doc. |

---

## 4. `dashboard/`

| File | Verdict | Reason |
|------|---------|--------|
| `dashboard/README.md` | **Keep** | Per-folder readme. |
| `dashboard/STREAMLIT_BUGS_REPORT.md` | **Could delete** | Bug audit; useful until bugs are fixed, then optional. |

---

## 5. `dashboard/pages/` (AI Scout)

| File | Verdict | Reason |
|------|---------|--------|
| `AI_Scout_agent_prompt.md` | **Keep** | Referenced by agent prompt; describes how to use test questions. |
| `AI_Scout_test_questions.md` | **Keep** | Input for `run_ai_scout_tests.py`. |
| `AI_Scout_test_questions_v2.md` | **Keep** | Same (v2 question set). |
| `AI_Scout_test_results.md` | **Should delete** | Generated by `run_ai_scout_tests.py`; regeneratable; don’t commit test outputs. |
| `AI_Scout_test_results_v2.md` | **Should delete** | Same. |

---

## 6. `dashboard/scouts/`

| File | Verdict | Reason |
|------|---------|--------|
| `POLISH_PLAN_PROFESSIONAL_SCOUTING.md` | **Keep** | Living checklist for scouts polish (you asked for it). |
| `PROFILE_IMPROVEMENTS_100.md` | **Could delete** | Backlog; optional to keep. |
| `SCOUTS_300_IMPROVEMENTS.md` | **Could delete** | Backlog; optional. |
| `compare_list_scouts.json` | **Should not be in repo** | User state; already in `.gitignore`. If it was ever committed, remove with `git rm --cached`. |

---

## 7. `dashboard/tactics/`

| File | Verdict | Reason |
|------|---------|--------|
| `DATA_QUALITY.md` | **Could delete** | Data quality notes. |
| `IMPROVEMENTS_300.md` | **Could delete** | Backlog. |
| `IMPROVEMENTS_CRITICAL_ASSESSMENT.md` | **Could delete** | Assessment doc. |
| `TACTICAL_PROFILE_IMPROVEMENT_PLAN.md` | **Could delete** | Plan doc. |

---

## 8. `dashboard/ux_audit_results/`

| Item | Verdict | Reason |
|------|---------|--------|
| `dashboard/ux_audit_results/` (entire folder) | **Should delete** | Screenshots from UX audit (~200+ PNGs). Regeneratable; bloats the repo. Add `dashboard/ux_audit_results/` to `.gitignore` if you might regenerate and don’t want to commit. |

---

## 9. `dashboard/review/`

| File | Verdict | Reason |
|------|---------|--------|
| `schedule_priorities.json` | **Should not be in repo** | User state; in `.gitignore`. If committed, `git rm --cached`. |

---

## 10. `config/`

No `config/README.md` in the list; PUBLISH.md expects one. **Keep** all YAML; add a short `config/README.md` if you want (optional).

**Keep:** `env.yaml`, `scope.yaml`, `competitions.yaml`.

---

## 11. `viz/`

| Item | Verdict | Reason |
|------|---------|--------|
| `viz/README.md` | **Keep** | |
| `viz/output/` | **Could ignore** | Generated output. Add `viz/output/` to `.gitignore` if you don’t want to commit it. |
| `viz/.DS_Store` | **Delete** | OS file; should be in `.gitignore` (`.DS_Store` already is). |

---

## 12. `export/`

**Keep:** `export/README.md`, `export/scripts/` (if it’s code). Nothing to delete.

---

## 13. `web/`

- **Keep:** Source (e.g. `src/`, `index.html`, `vite.config.ts`, `package.json`, etc.), `web/README.md`.
- **Already in .gitignore:** `web/dist/`, `web/node_modules/`, `web/*.tsbuildinfo`, `web/vite.config.d.ts`. Do not commit these; delete from disk only if you want to free space locally (they are regeneratable).

---

## 14. Other files / folders

- **`.cursor/`** — In `.gitignore`; don’t commit. Can delete locally if you don’t need Cursor state.
- **`.pytest_cache/`** — In `.gitignore`; don’t commit. Safe to delete locally.
- **`data/`** — `data/raw/`, `derived/`, `processed/`, `logs/`, `index/` are in `.gitignore`. Don’t commit. Do not delete from disk if you need them to run the app; they stay local only.
- **`output/`** — In `.gitignore`; empty or generated. Don’t commit.

---

## 15. Summary — recommended actions

### Should do (recommended)

1. **Delete from disk (and ensure not committed):**
   - Root: `Microsoft Word - A Poisson Betting Model...pdf`, `dq_check_stdout.txt`, `qa_extended_checks.json`, `validation_data_verbose.txt`
   - `dashboard/pages/AI_Scout_test_results.md`, `dashboard/pages/AI_Scout_test_results_v2.md`
   - `dashboard/ux_audit_results/` (entire folder)

2. **Ensure ignored / remove from index if ever committed:**
   - `dashboard/scouts/compare_list_scouts.json`
   - `dashboard/review/schedule_priorities.json`

3. **Add to `.gitignore`** (if not already):
   - `dashboard/ux_audit_results/`
   - `dashboard/pages/AI_Scout_test_results*.md` (optional; so future test runs don’t commit results)
   - `viz/output/` (optional)

### Could do (optional cleanup)

- Delete `archive/` (empty).
- Delete `Tactical_Preparation_Plan_Next_Opponent.md` if you don’t want it in the repo.
- Delete docs: `DATA_QA_FIX_PLAN.md`, `DATA_QA_FIX_PLAN_FOOTBALL_REFINED.md`, `DATA_QA_REPORT.md`, `DATA_QA_MEGA_PROMPT.md`, `DATA_AND_PLAN.md`, `DASHBOARD_UI.md`, `PROMPT_UI_OVERHAUL.md`.
- Delete dashboard markdowns: `STREAMLIT_BUGS_REPORT.md`, scouts `PROFILE_IMPROVEMENTS_100.md`, `SCOUTS_300_IMPROVEMENTS.md`, tactics `DATA_QUALITY.md`, `IMPROVEMENTS_300.md`, `IMPROVEMENTS_CRITICAL_ASSESSMENT.md`, `TACTICAL_PROFILE_IMPROVEMENT_PLAN.md`.

---

## 16. Commands to run (after you confirm)

```bash
# From repo root

# 1. Delete root artifacts and PDF
rm -f "Microsoft Word - A Poisson Betting Model with a Kelly Criterion Element for European Soccer.docx - Unknown 21.26.55.pdf"
rm -f dq_check_stdout.txt qa_extended_checks.json validation_data_verbose.txt

# 2. Delete generated AI Scout test results
rm -f dashboard/pages/AI_Scout_test_results.md dashboard/pages/AI_Scout_test_results_v2.md

# 3. Delete UX audit screenshots folder
rm -rf dashboard/ux_audit_results

# 4. Optional: empty archive
rmdir archive 2>/dev/null || true

# 5. If any of the above were already committed:
# git rm --cached "path/to/file"
# git rm -r --cached dashboard/ux_audit_results
```

Then add to `.gitignore` if you want:

```
dashboard/ux_audit_results/
dashboard/pages/AI_Scout_test_results.md
dashboard/pages/AI_Scout_test_results_v2.md
viz/output/
```
